<% form_for :extra, :url => { :action => 'create' } do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form' %>
  <p> <%= submit_tag "Create" %> </p>
<% end %>
<%= set_focus_to_id 'extra_name' %>
<%= button_to 'Cancel', :action => 'list' %>
